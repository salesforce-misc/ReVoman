$kind: http-request
description: Links the Availability rule to the policy.
url: "{{baseUrl}}{{versionPath}}/tooling/sobjects/SchedulingPolicyRule"
method: POST
headers:
  Content-Type: application/json
  Accept: application/json
body:
  type: json
  content: |-
    {
      "SchedulingPolicyId": "{{territoryPartialPolicyId}}",
      "SchedulingRuleId": "{{territoryPartialAvailabilityRuleId}}",
      "IsActive": true
    }
scripts:
  - type: afterResponse
    code: |-
      pm.environment.set("territoryPartialAvailabilityPolicyRuleId", pm.response.json().id);
    language: text/javascript
order: 50000
