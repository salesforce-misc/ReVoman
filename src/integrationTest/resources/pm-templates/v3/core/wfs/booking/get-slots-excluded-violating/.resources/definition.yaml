$kind: collection
description: >-
  Rules parity (ExcludedResources) READ violating — GetAppointmentSlots for the required+primary resource
  that is on the appointment Account's excluded list (a ResourcePreference PreferenceType=Excluded) under
  the excluded-resources policy. ExcludedResources prunes it, so the read offers ZERO slots. Paired with
  the write violating act to prove read==write on ExcludedResources.
auth:
  - id: 3c2d1e00-0001-4aaa-9bbb-000000000001
    type: bearer
    name: bearer auth
    credentials:
      token: "{{managerToken}}"
