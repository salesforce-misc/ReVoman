$kind: collection
description: >-
  Rules parity (WorkingLocations / SchedulingRuleType WorkingTerritories) WRITE control — Schedule the
  required+primary resource that is a PRIMARY ServiceTerritoryMember into the window the read offered
  slots for → Success. WorkingLocations passes on both read and write (loadSchedulableSlots shared), so
  the write keeps the read's promise. This is the positive half of the WorkingLocations read==write
  matrix. Runs LAST (it persists an SA).
auth:
  - id: 4d3e2f00-0004-4aaa-9bbb-000000000004
    type: bearer
    name: bearer auth
    credentials:
      token: "{{managerToken}}"
