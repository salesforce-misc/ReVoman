$kind: collection
description: |-
  Decision 3 Act B (doc L142 control) — schedule a SINGLE assigned resource with isRequiredResource=true
  and NO isPrimaryResource, on FRESH resources, under the availability-op-hours policy (Availability +
  WorkingTerritories, NO RequiredResources rule). isPrimaryResource is multi-resource-only plumbing, NOT
  the Decision-1 variable, so this MUST be a valid Schedule (schedulingStatus=Success) — the clean
  counterpart to Act A's missing-flag crash. Runs as the manager persona. Assertion lives in the Java test.
auth:
  - id: 7a1b2c3d-4e5f-4061-8a2b-3c4d5e6f7a1c
    type: bearer
    name: bearer auth
    credentials:
      token: "{{managerToken}}"
