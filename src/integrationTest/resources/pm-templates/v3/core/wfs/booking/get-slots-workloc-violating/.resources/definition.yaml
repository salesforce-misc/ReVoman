$kind: collection
description: >-
  Rules parity (WorkingLocations / SchedulingRuleType WorkingTerritories) READ violating —
  GetAppointmentSlots for the required+primary resource that is a SECONDARY ServiceTerritoryMember
  (TerritoryType=S) under the territory-membership-partial policy (WorkingTerritories +
  IsPrimaryLocationEnabled=true, which includes PRIMARY members only). WorkingLocations prunes the
  Secondary member, so the read offers ZERO slots. Paired with the write violating act to prove
  read==write on WorkingLocations.
auth:
  - id: 4d3e2f00-0001-4aaa-9bbb-000000000001
    type: bearer
    name: bearer auth
    credentials:
      token: "{{managerToken}}"
