$kind: collection
description: |-
  Unified prior-assignment schedule (schedule-prior-appt2-b-primary — see the request file). Runs under {{adminToken}}.
auth:
  - id: 7e1f0c00-1005-4aaa-9bbb-000000000048
    type: bearer
    name: bearer auth
    credentials:
      token: "{{adminToken}}"
