$kind: collection
description: >-
  Rules parity (WorkingLocations / SchedulingRuleType WorkingTerritories) READ control — the
  required+primary resource is a PRIMARY ServiceTerritoryMember (TerritoryType=P), which the
  WorkingTerritories(IsPrimaryLocationEnabled=true) policy includes → WorkingLocations passes → read
  offers >0 slots (proves the fixture is valid, so the violating 0 is the rule). Paired with the write
  control act to prove read==write on WorkingLocations.
auth:
  - id: 4d3e2f00-0002-4aaa-9bbb-000000000002
    type: bearer
    name: bearer auth
    credentials:
      token: "{{managerToken}}"
