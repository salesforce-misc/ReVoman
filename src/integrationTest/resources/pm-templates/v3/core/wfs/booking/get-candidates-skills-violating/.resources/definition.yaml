$kind: collection
description: >-
  Cross-API agreement (MatchSkills) READ via GetAppointmentCandidates — the SAME required+primary
  resource that LACKS the WorkType's required skill under the match-skills policy, body identical to
  the get-appointment-slots violating act but hitting the get-appointment-candidates action. Proves
  this read API runs the same loadSchedulableSlots engine: MatchSkills prunes the skill-lacking
  resource so ZERO candidates come back. Response is result[].candidates[]. Runs as {{managerToken}}
  (the least-privilege manager persona that owns the fixture). Stashes skillsCandidatesCount;
  assertions live in the Java test.
auth:
  - id: 8b7c6d00-0001-4aaa-9bbb-000000000001
    type: bearer
    name: bearer auth
    credentials:
      token: "{{managerToken}}"
