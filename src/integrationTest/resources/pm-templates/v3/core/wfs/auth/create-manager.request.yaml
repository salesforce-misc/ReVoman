$kind: http-request
description: Creates a user and assigns them with the necessary perm sets
url: "{{baseUrl}}{{versionPath}}/composite/graph"
method: POST
body:
  type: json
  content: |-
    {
      "graphs": [
        {
          "graphId": "1",
          "compositeRequest": [
            {
              "method": "POST",
              "url": "{{versionPath}}/sobjects/User",
              "referenceId": "refUser",
              "body": {
                "Username": "{{managerUserName}}",
                "ProfileId": "{{standardUserProfileId}}",
                "Alias": "sarah",
                "Email": "{{managerUserName}}",
                "EmailEncodingKey": "ISO-8859-1",
                "LastName": "Smith",
                "FirstName": "Sarah",
                "LanguageLocaleKey": "en_US",
                "LocaleSidKey": "en_US",
                "TimeZoneSidKey": "Asia/Kolkata"
              }
            },
            {
              "method": "POST",
              "url": "{{versionPath}}/sobjects/PermissionSetAssignment/",
              "referenceId": "unifiedPS1Assignemnt",
              "body": {
                "AssigneeId": "@{refUser.id}",
                "PermissionSetId": "{{unifiedPSManagerId}}"
              }
            }
          ]
        }
      ]
    }
scripts:
  - type: beforeRequest
    code: pm.environment.set("managerUserName", pm.variables.replaceIn("manager-{{$timestamp}}@revoman.org"));
    language: text/javascript
  - type: afterResponse
    code: |-
      var jsonData = pm.response.json()
      pm.environment.set("caseManagerUserId", jsonData.graphs[0].graphResponse.compositeResponse[0].body.id);
    language: text/javascript
order: 17000
