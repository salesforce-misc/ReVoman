$kind: http-request
url: "{{baseUrl}}{{versionPath}}/tooling/sobjects/SchedulingPolicyRule"
method: POST
headers:
  Content-Type: application/json
  Accept: application/json
body:
  type: json
  content: |-
    {
      "SchedulingPolicyId": "{{excludedResourcesPolicyId}}",
      "SchedulingRuleId": "{{excludedResourcesExcludedRuleId}}",
      "IsActive": true
    }
scripts:
  - type: afterResponse
    code: |-
      pm.environment.set("excludedResourcesExcludedPolicyRuleId", pm.response.json().id);
    language: text/javascript
order: 48000
