$kind: http-request
description: Decision 9 — link the Availability rule to the policy (tooling SchedulingPolicyRule).
url: "{{baseUrl}}{{versionPath}}/tooling/sobjects/SchedulingPolicyRule"
method: POST
headers:
  Content-Type: application/json
body:
  type: json
  content: |-
    { "SchedulingPolicyId": "{{dec9PolicyId}}", "SchedulingRuleId": "{{dec9AvailabilityRuleId}}", "IsActive": true }
order: 40000
