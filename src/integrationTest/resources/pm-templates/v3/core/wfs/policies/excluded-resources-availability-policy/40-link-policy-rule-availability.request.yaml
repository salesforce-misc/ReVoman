$kind: http-request
url: "{{baseUrl}}{{versionPath}}/tooling/sobjects/SchedulingPolicyRule"
method: POST
headers:
  Content-Type: application/json
  Accept: application/json
body:
  type: json
  content: |-
    {
      "SchedulingPolicyId": "{{excludedResourcesPolicyId}}",
      "SchedulingRuleId": "{{excludedResourcesAvailabilityRuleId}}",
      "IsActive": true
    }
scripts:
  - type: afterResponse
    code: |-
      pm.environment.set("excludedResourcesAvailabilityPolicyRuleId", pm.response.json().id);
    language: text/javascript
order: 40000
