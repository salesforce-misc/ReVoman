$kind: http-request
url: "{{baseUrl}}{{versionPath}}/tooling/sobjects/SchedulingPolicyRule"
method: POST
headers:
  Content-Type: application/json
  Accept: application/json
body:
  type: json
  content: |-
    {
      "SchedulingPolicyId": "{{requiredResourcesPolicyId}}",
      "SchedulingRuleId": "{{requiredResourcesRequiredRuleId}}",
      "IsActive": true
    }
scripts:
  - type: afterResponse
    code: |-
      pm.environment.set("requiredResourcesRequiredPolicyRuleId", pm.response.json().id);
    language: text/javascript
order: 48000
