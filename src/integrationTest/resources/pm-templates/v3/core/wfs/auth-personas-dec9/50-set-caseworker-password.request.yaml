$kind: http-request
description: Decision 9 — set the case-worker persona's password for its own SOAP session.
url: "{{baseUrl}}{{versionPath}}/sobjects/User/{{dec9CaseWorkerUserId}}/password"
method: POST
headers:
  Content-Type: application/json
body:
  type: json
  content: |-
    { "NewPassword": "{{dec9PersonaPassword}}" }
order: 50000
