$kind: collection
description: |-
  OLD Scheduler prior-assignment booking, single-resource appt #2 (service-appointments-prior-appt2-b-single — see the request file). Runs under {{adminToken}}.
auth:
  - id: 7e1f0c00-1005-4aaa-9bbb-000000000048
    type: bearer
    name: bearer auth
    credentials:
      token: "{{adminToken}}"
