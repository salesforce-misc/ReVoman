$kind: collection
description: |-
  SUPPLEMENTAL grant for the THIRD resource (C) of the 3-resource prior-assignment(-concurrent)
  fixtures. The shared grant (booking/grant-ls-user-access) only grants resourceA + resourceB — it was
  cloned from the 2-resource double-book fixture and never extended to C (appt #2's dedicated primary),
  so on the REST path SchedulingServiceImpl.removeResourcesWithoutPerm prunes resourceC for lacking the
  lightningSchedulerUserAccess perm, masking the occupancy/overbooking outcome under test. Assigns C the
  same Lightning Scheduler PSL + Salesforce_Scheduler_Resource permission set, reusing lsPslId / lsPsId
  captured by the shared grant's step 05. RevUp'd right after the shared grant Kick. Runs under
  {{adminToken}}.
auth:
  - id: 7e1f0c00-1005-4aaa-9bbb-00000000002c
    type: bearer
    name: bearer auth
    credentials:
      token: "{{adminToken}}"
