$kind: collection
description: |-
  OLD Scheduler getAppointmentSlots over the excluded-helper fixture, WITH accountId, naming only the
  account-Excluded resourceB as required. Proves the Excluded list is NOT applied on the slots path:
  resourceB (available) returns >0 slots even though it is on the account's Excluded list, unlike the
  getAppointmentCandidates read which prunes it. Captures oldSlotsExcludedBWithAcctCount. Runs under
  {{adminToken}}.
auth:
  - id: 7e1f0c00-1005-4aaa-9bbb-000000000044
    type: bearer
    name: bearer auth
    credentials:
      token: "{{adminToken}}"
