$kind: collection
description: |-
  OLD Scheduler prior-assignment booking (service-appointments-prior-appt1-b-optional — see the request file). Runs under {{adminToken}}.
auth:
  - id: 7e1f0c00-1005-4aaa-9bbb-000000000042
    type: bearer
    name: bearer auth
    credentials:
      token: "{{adminToken}}"
