$kind: http-request
name: Evergreen PSM
url: "{{baseUrl}}{{versionPath}}/query/?q=SELECT Id, SellingModelType, PricingTerm, PricingTermUnit, Status FROM ProductSellingModel where SellingModelType = 'Evergreen'"
method: GET
headers:
  Content-Type: application/json
queryParams:
  q: SELECT Id, SellingModelType, PricingTerm, PricingTermUnit, Status FROM ProductSellingModel where SellingModelType = 'Evergreen'
body:
  type: text
  content: ""
scripts:
  - type: afterResponse
    code: |-
      var jsonData = pm.response.json()
      pm.environment.set("evergreenProductSellingModelId", jsonData.records[0].Id);
    language: text/javascript
order: 3000
