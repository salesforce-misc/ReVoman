$kind: http-request
url: "{{baseUrl}}{{versionPath}}/query/?q=SELECT Id FROM Pricebook2 WHERE IsStandard = true"
method: GET
headers:
  Content-Type: application/json
  Sforce-Call-Options: client=SfdcInternalQA/...
queryParams:
  q: SELECT Id FROM Pricebook2 WHERE IsStandard = true
body:
  type: text
  content: ""
scripts:
  - type: afterResponse
    code: |-
      var jsonData = pm.response.json()
      pm.environment.set("standardPricebookId", jsonData.records[0].Id);
    language: text/javascript
order: 1000
