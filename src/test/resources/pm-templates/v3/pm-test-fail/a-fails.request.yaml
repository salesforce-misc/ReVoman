$kind: http-request
name: a-fails
url: "{{baseUrl}}/a"
method: GET
scripts:
  - type: afterResponse
    code: |-
      pm.test('intentionally fails', () => pm.expect(pm.response.code).to.eql(999));
    language: text/javascript
order: 1000
